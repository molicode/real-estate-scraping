[pytest]
# Solo la carpeta tests/. scripts/ tiene diagnósticos ejecutables que empiezan
# con "test_" (ej. test_playwright.py) y mutan variables de entorno al importar;
# si pytest los colectara, contaminarían al resto de la suite.
testpaths = tests
